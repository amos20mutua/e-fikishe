# E‑Fikishe — Rebuilt demo

This workspace was refreshed into a modern, parallax-first static demo.

What I changed
- Replaced previous multi-page site with a single modern landing: `index.html`.
- New styles in `styles.css` (palette: fire orange, black, gold, teal).
- New `script.js` with a lightweight, performant parallax module and basic interactivity.

How to preview
- Open `index.html` in your browser. No server required.

Next steps
- Tune copy, add images, and add production assets (logo, favicon).
- If you want me to restore any old content, I can extract or recreate it.
# E-Fikishe — Investor-ready landing (static)

Quick static landing for the Nairobi-based electric motorbike last-mile delivery platform.

How to use
- Open `index.html` in your browser (no server required).
- Edit copy in `index.html` and styles in `styles.css`.

Files added
- `index.html` — mobile-first landing page with hero, impact, gallery, and contact.
- `styles.css` — responsive styles, accent: electric-cyan.
- `script.js` — small interactions: nav toggle, scroll reveal, contact form stub.

Next recommended steps
- Add a hosted investor deck (link to PDF) and a simple backend or form handler for contact submissions.
- Replace placeholder metrics with live data.
- Create high-res assets (logo SVG) and favicon.
